<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="atomic0x90.github.io.mathgame">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".InfinityPlusMenu"></activity>
        <activity android:name=".Challenge_divide" />
        <activity android:name=".Challenge_multi" />
        <activity android:name=".Challenge_sub" />
        <activity android:name=".SplashScreen" />
        <activity android:name=".UserStatistics" />
        <activity
            android:name=".CustomDialog"
            android:screenOrientation="fullSensor"
            android:theme="@style/popupTheme" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Challenge_add" />

        <meta-data
            android:name="com.google.android.gms.ads.AD_MANAGER_APP"
            android:value="true" />
    </application>

</manifest>